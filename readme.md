## Deployment
Deployment process:

Required software:
* terminal for running bash scripts
* maven 3
* docker
* docker-compose

to deploy application, switch to needed branch and run bash script:
$ bash start.sh
